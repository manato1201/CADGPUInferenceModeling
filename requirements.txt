# ── 入力・パース ──────────────────────────
ezdxf>=1.3.0          # DXF/DWG パース

# ── 画像処理・レンダリング ─────────────────
matplotlib>=3.8.0     # 三面図レンダリング
Pillow>=10.0.0        # 画像操作
numpy>=1.26.0
opencv-python>=4.9.0  # 前処理フィルタリング

# ── 3D後処理 ──────────────────────────────
open3d>=0.18.0        # メッシュ操作・LOD
trimesh>=4.3.0        # glTF/OBJ エクスポート
pyglet<2              # trimesh の可視化依存

# ── GPU推論（Zero123++）────────────────────
torch>=2.2.0          # CUDA対応版は別途インストール
torchvision>=0.17.0
diffusers>=0.27.0     # Zero123++ はdiffusersベース
transformers>=4.38.0
accelerate>=0.27.0

# ── API化フェーズ2用（今は不要だがpinしておく）──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
celery>=5.3.0
redis>=5.0.0
python-multipart>=0.0.9

# ── CLI ───────────────────────────────────
typer>=0.12.0
rich>=13.7.0          # ターミナル出力整形
